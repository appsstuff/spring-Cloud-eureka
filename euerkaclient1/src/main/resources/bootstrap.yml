spring:
  application:
    name: client1
  cloud:
    config:
      uri:  http://config-server:8008
      enabled: true      
      fail-fast: true
management:
 endpoints:
   web:
    exposure:
     include: "*"