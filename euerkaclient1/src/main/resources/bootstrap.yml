spring:
  application:
    name: client1
  cloud:
    config:
      uri:  http://localhost:8008
      enabled: true      
      fail-fast: true
management:
 endpoints:
   web:
    exposure:
     include: "*"